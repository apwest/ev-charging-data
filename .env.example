# Weather fetch (scripts/weather.py). Copy to .env and fill in; .env is gitignored.
# In GitHub Actions these come from repository secrets, not this file.
WEATHER_API_KEY=your-weather-com-api-key
WEATHER_LOCATION=KOWD:9:US
